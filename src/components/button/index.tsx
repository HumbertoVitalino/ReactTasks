import React from 'react';

function Button() {
    const backgroundColor = 'green';
    const styles = {
        backgroundColor
    }
    return (
        <button style={styles}>
            Button
        </button>
    )
}

export default Button;